program -> statment

statment -> TypeVar > expresion
            TypeVarVarios > funcion (expresion) 
            funcion ( expresion )
            for ( ID in ID ) { statment }
            if ( condicion ) { statment } else { statment } 


TypeVar -> type ID

TypeVarVarios -> TypeVar, TypeVarVarios
                 E


expresion -> expresion operador expresion
            expresion comparacion expresion
            ID
            valor
            

funcion -> crack
          mvp
          localiza
          saca
          wachea



valor -> 0...9
          "string"
          'a'...'z'

type -> str
        char
        int
        bool

comparacion -> mayorq
              menorq
              igualq
              difq

operador -> +
            -
